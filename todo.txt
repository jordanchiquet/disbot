- Biden I did That image overlay thing
- count graphs
- open up timer input (make .timer 5 pm without colon work)
- feedback rewrite - needs to include messages before it to be useful 
- hook up gdrive stuff, make a way for logans audio clips for example to go to quotes
- wordcloud
- translate 
- backup sql db (and make automatic)
- push/snooze for timers; hours as primary arg, floats (e.g. push .5) sets dupe of timer that just popped 30 m from original
- bot adding extra blank line in todo test:
    ".todo test" appends one blank line and then "- test" to this file
- bot not adding to that file unless ran as sudo, some kind of perm writing issue most likely 

- img resizer
