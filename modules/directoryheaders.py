import os

print (os.name) # nt on windows 10/ posix on ubuntu

osname = os.name

directorydict = {
    
    
}

# decide if i want to just use environent variables or instead actually hard code two separate dictionaries and grab them here based on os.name